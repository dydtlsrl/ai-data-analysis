product_name = "파이썬기초" #str
price = 28000 #int
discount_rate = 0.15 #float
is_on_sale = True #bool
stock = 12 #int

price_text = str(price)

print(type(product_name))
print(type(price))
print(type(discount_rate))
print(type(is_on_sale))
print(type(stock))

int("파이썬") #정수로 바꿀 수 없는 문자열을 바꾸려고 했기 때문에 오류가 발생

# Q1. 1.값을 재사용하기 위해서
#     2.값이 바뀌어도 한 곳만 고치면 되기 때문에
#     3.코드의 뜻을 직관적으로 알수 있게 하기 위해서
#     4.계산 결과, 사용자의 입력 값처럼 미리 알 수 없는 값을 담아두기 위해

# Q2. 1.age >>> 변수명
#     2. = >>> 대입연산자( 오른쪽의 값을 왼쪽 변수에 저장/대입 한다.)
#     3. 20 >>> 변수의 값

# Q3. user_name
#     놓쳤던 답 >>> score2

# Q4. 